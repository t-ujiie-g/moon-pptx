.DS_Store
_build/
.mooncakes/
.moonagent/
.claude/

# Generated artefacts. `.pptx` files emitted by ad-hoc test runs and
# the `out/` directory used during development should stay out of git.
out/
*.pptx
# …but the curated real-world verification corpus is tracked on purpose.
!test_fixtures/corpus/*.pptx

# .NET build output for the pptx-validate tool (tools/pptx-validate/).
tools/pptx-validate/bin/
tools/pptx-validate/obj/

# PptxGenJS is installed on demand by tools/bench/run.sh, not vendored.
tools/bench/node_modules/
tools/bench/package-lock.json
tools/bench/package.json
